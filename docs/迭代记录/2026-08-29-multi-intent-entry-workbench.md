# 多意图工作台入口改造记录

- **事项**：PPT-UI-002
- **路线**：direct
- **状态**：已完成
- **关联方向**：`docs/issues/todo.md` 的“前端创作与交付工作台”

## 本轮变更

- 默认 `/` 进入 Dashboard 工作台，默认以普通对话呈现。
- 自然语言仍由既有 `chat/create/plan/fix` 意图路由自动识别；用户不需要先理解或切换工作模式。
- 输入框底部保留轻量的“PPT 生成”能力入口。点击后，下一条消息会明确按 PPT 创建处理，即使消息文本没有出现“PPT”字样；取消后立即恢复自动识别。

## 验证与部署

- 本地：`go test ./pkg/web` 通过；`npm test -- --run` 通过（39/39）；`npm run build` 与 Linux 后端构建通过。
- 已部署：替换 `remote-dev:/ppt/ppt-agent-linux` 和 `frontend/dist`，保留备份 `ppt-agent-linux.bak.20260829-intent-tool`、`dist.bak.20260829-intent-tool`；新进程 PID `383856`。
- 公网验收地址：`http://124.220.22.162:8080/`；浏览器已登录页面显示无顶部模式切换的通用对话页和底部“PPT 生成”入口。`/api/health`、`/`、`/dashboard` 均为 200，启动日志显示 MySQL 已连接且服务监听 `:8080`。

## 阻塞与后续

此前使用的错误地址 `20.22.0.162:8080` 会返回 HTTP 503；用户已确认主机为 `124.220.22.162`。实测可用公网地址为 `http://124.220.22.162:8080/`，其 80 端口当前未监听；后续验收统一使用 `8080` 端口。

## 追加：闲聊 SSE 修复

- 问题：已有会话的第二条 `chat` 路由直接返回，前端没有 SSE 终态，导致输入一直禁用；短回复的 SSE 连接关闭还可能错过末尾事件并显示“正在恢复实时连接”。
- 修复：闲聊回复统一写入任务事件流，以 `conversation_complete` 结束；模型生成后台执行、答案分块发出，35 秒超时兜底；前端在短流关闭时重读已持久化的会话结果并结束本轮。
- 验证：`go test ./pkg/web ./pkg/task`、`go build ./...`、前端 40 项测试和构建通过。2026-08-29 已部署到 `remote-dev:/ppt/ppt-agent`，进程 PID `397999`，最后一版 `frontend/dist` 已替换；公网 `/api/health` 返回 200；Edge 中同一会话连续两轮闲聊均完成并可继续输入，空闲对话不再显示“正在思考”。
