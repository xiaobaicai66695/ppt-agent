# Web handlers

Web 代码按业务职责导航。由于 Go package 不能跨目录共享未导出符号，当前实现文件仍保持在本目录；以下子目录是稳定的职责索引，新增代码按归属放置并在下一次边界迁移时整体抽取为子包。

- `auth/`：登录、注册、会话鉴权
- `task/`：任务创建、查询、取消、反馈
- `conversation/`：对话与继续任务
- `delivery/`：SSE、下载、缩略图
- `admin/`：凭据、日志分析、管理接口
